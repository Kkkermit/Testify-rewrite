const { getLatestVersion, checkVersion } = require('./versionHandler/version')
const { asciiText } = require('./asciiText/asciiText')

module.exports = {
    getLatestVersion,
    checkVersion,
    asciiText,
};